<!DOCTYPE form-validation PUBLIC
"-//Apache Software Foundation//DTD Commons Validator Rules Configuration 1.1.3//EN"
"http://jakarta.apache.org/commons/dtds/validator_1_1_3.dtd">
<form-validation>
    <formset>
        <form name="/CreatePrivateMessageForm">
            <field property="messageTo" depends="required">
                <arg key="privatemessages.error.messageTo" />
            </field>
            <field property="messageTo" depends="required">
                <arg key="privatemessages.error.messageSubject" />
            </field>
            <field property="messageBody" depends="required">
                <arg key="privatemessages.error.messageBody" />
            </field>
        </form>
    </formset>
</form-validation>